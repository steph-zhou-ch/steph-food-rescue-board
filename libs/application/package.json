{
  "name": "@app/application",
  "version": "0.0.0",
  "private": true,
  "description": "Application layer — use cases that orchestrate domain logic. Pure TypeScript, no framework dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run src"
  },
  "dependencies": {
    "@app/domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "typescript": "5.5.3",
    "vitest": "2.1.4"
  }
}
