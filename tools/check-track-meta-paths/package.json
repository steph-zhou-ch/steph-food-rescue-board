{
  "name": "@charliehealth/check-track-meta-paths",
  "version": "0.0.0",
  "private": true,
  "description": "Read-only gate that fails the build if any track-meta's `deliverables:` paths fall outside the engagement's stack-specific roots (apps/, libs/, migrations/, tools/, orchestration/, contracts/, docs/).",
  "type": "module",
  "bin": {
    "check-track-meta-paths": "./src/check.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run --dir test/integration --passWithNoTests"
  },
  "dependencies": {
    "yaml": "2.6.1"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "typescript": "5.5.3",
    "vitest": "2.1.4"
  }
}
