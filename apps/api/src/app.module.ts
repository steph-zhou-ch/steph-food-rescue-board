import { Module } from '@nestjs/common';

/**
 * Root NestJS module. Capability tracks add controllers + providers here.
 * Starts empty — the first wave populates it.
 */
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class AppModule {}
